# Next Open Jira
Instalaciones necesarias
  mongo-compass
  docker-desktop

Para correr localmente, you need database
```
docker-compose up -d
```
* el -d = __detached__

## Mongo Connect
```
mongodb://localhost:27018/entriesdb
```